# Pocket 3 Controller 支援矩陣（唯一 current source of truth）

更新：2026-09-11。這份文件是目前 Pocket 3 支援度的唯一狀態表；它把機身能力、App UI、App/Core 讀寫、transport、實機證據和 release 邊界分開記錄。**有畫面、存在 API、送出命令或取得 ACK，都不單獨等於功能完成，更不等於機身已實際動作。** 未知保持 `unknown`，不以其他 DJI 型號、開源候選或舊版結果補齊。

## 版本與證據邊界

| 項目 | current 狀態 |
|---|---|
| 公開版本 | `0.0.1 beta 2`、build 24，tag `v0.0.1-beta.2`。beta 1 build 9、build 22 benchmark 和公開 screenshots 都是歷史證據。 |
| build 24 軟體 gate | beta 2 的 exact-source gate 通過：source `33d9a693255afa8a8b93a71e086cd4cc782a5f4e`，App SHA-256 `9bafcb7636e8c1d9f9fbe0b661892ad65fe730f6fb49faa6370255e2fe2734e0`；537 listed、534 executed、3 opt-in skip（Intelligence 44、Evaluation 1、Core 420、App 72），59 UI、三語、7 Yun、複製 App 的 MLX/CoreAI、ZIP/DMG 與簽章均通過。build 23 的媒體驗證是其中一部分證據，不是 build 24 的來源版本。這是軟體／封裝 gate，不是所有機身功能的通行證。 |
| build 23 媒體／真機邊界 | 媒體 gate 使用合成 image/video；ROI MLX count 1 有 `uncertain=true` 警告、Vision OCR `FRAME B`、Apple answer `FRAME B`、JSON/Markdown export、rapid seek cancel 後實際時間 1.5 s、三語 redacted UI。離線期間 camera frames、session、access 維持不變。真機只通過有界 observe／assistFraming：MLX 讀取工具、zoom raw 110→獨立讀回 109（容許 1）、restore 100；Apple observe 遇系統 safety guardrail 並完成 cleanup。 |
| 使用者確認的韌體 | 整機 `01.06.10.04`；Camera `10.00.50.51`；Gimbal `01.00.15.81`。USB `bcdDevice=0x0504` 不是 firmware version。 |
| build 25 | `main` 已加入 `02/80` camera status、`02/DC` storage、九類唯讀 camera properties、被動 tracking candidate 紀錄、source switch 的 `remoteTask` fence、UVC control-read 降級處理和 identity UI wording。Release tests 634 次執行零失敗；USB 1080p30 與 BLE 唯讀 live 對照已完成，完整 release gate 尚待完成。 |
| 讀表方法 | `App UI` 表示使用者可見入口；`App/Core 讀／API` 與 `App/Core 寫／API` 先回答 source/API 是否存在（有、無、WIP 或 gated）；`實機證據` 再回答 read/write 是否真的測過，以及是否有 physical result。`release 狀態` 不會因 API 存在自動變成可用。 |

證據標籤的意思如下：`實機通過（有界）` 只涵蓋列出的模式、值、session 和 cleanup；`實機 read-only` 只證明讀到資料，不證明 setter；`實機未通過／未確認` 表示有實際嘗試但沒有完成條件；`離線 gate` 是 fixture、合成媒體或複製 App；`source/UI only` 只有程式或 UI 存在；`unknown` 表示尚無足夠證據。

## Current support matrix

| 功能 | 機身事實 | App UI | App/Core 讀／API | App/Core 寫／API | transport | 實機證據 | release 狀態 | 完成條件 | 最小下一 probe |
|---|---|---|---|---|---|---|---|---|---|
| USB Webcam 模式、preview、權限與 session | Pocket 3 官方支援 Webcam；本機可列出裝置的動態格式。 | 有裝置、模式、pixel format、access、pause/reconnect UI。 | 有 CaptureMode 動態枚舉、AVFoundation frame/session/freshness。`Sources/Pocket3Core/CaptureMode.swift:34-99`、`CameraService.swift:341-415`。 | 以 UVC/AVFoundation 重建 capture session；不是改機內錄影設定。 | USB UVC + AVFoundation。 | build25 metrics-only：13個 NV12 橫幅／直幅模式均通過 exact尺寸、420v、BGRA、新影格、freshness、session與FPS；rotation metadata皆0。沒有保存／檢視畫面，因此直幅內容方向與黑邊仍 unknown。UYVY與4K48/50/60尚未通過。 | NV12 stream metrics 實機通過（有界）；內容方向與高幀率仍未完成。 | 每個宣稱格式都要有新 frame、尺寸/fps/方向回讀、穩定 session、斷線清理。 | 用同一台相機逐一重測實際列出的格式、直式方向和黑邊；不把枚舉當 frame。 |
| USB pan / tilt 絕對目標 | UVC 宣告 raw pan/tilt 範圍；不等於 DJI 機械角度或原生 joystick。 | 有 slider、Home/Front/Back 和 manual access 條件；只有 preset 列需要 `defaultPosition`，slider 依 writable/range 啟用。 | 有 status、range、readback、fresh frame。`Sources/Pocket3Core/CameraService.swift:436-533,764-885`、`P3UVC.m:216-276`。 | 有 bounded absolute move；需 control access、session 和 readback。 | USB UVC raw control。 | build23 真機 gate 是 observe-only，沒有新的 pan/tilt physical acceptance；舊 manual records 僅歷史。 | API/UI 存在，physical movement 未完成。 | 每個方向和邊界都要有目標、readback、影像／姿態變化、timeout、stop、restore 證據。 | 受控地做一個小目標與一個邊界目標，記錄 frame、USB readback 和 physical result；不要自動擴大測試。 |
| USB continuous pan / tilt、release、Stop | App scheduler 以 successive absolute target 模擬連續操作；不是原生 velocity API。 | 有手勢、鍵盤、lease、Stop、focus 狀態。`Sources/Pocket3BridgeApp/ContinuousGimbalControls.swift:8-68`。 | 有 20 Hz scheduler、heartbeat、lease、neutral completion。`Sources/Pocket3Core/ContinuousGimbal.swift:63-214`。 | 有 USB absolute target、neutral、取消和 stale-session fences。 | USB UVC；`USBContinuousGimbalTransport.swift:13-16,128-227`。 | build23 observe gate 沒有測 continuous write；physical emergency stop／機械 latency 未證實。 | Experimental／bounded transport，不能稱 native joystick。 | 低負載長 hold、放開、Stop、斷線和失焦都要以實機影像／姿態和 neutral readback 通過。 | 先完成單一短 hold→release→Stop 的實機紀錄，明確量測 physical stop；不借用舊版結果。 |
| USB Zoom | 機身提供 UVC raw zoom；`100…400` 等數字不是校準的 x 倍率。 | 有 Zoom status、raw value、step、set、restore 和驗證訊息。`Sources/Pocket3BridgeApp/CameraZoomControls.swift:4-32`。 | 有 range/writable、readback tolerance、stable target window。`Sources/Pocket3Core/USBZoom.swift:42-115`、`CameraService.swift:527-649`。 | 有 bounded raw set，綁定 capture session 和 control access。 | USB UVC。 | build23 real hardware assistFraming：status→set raw 110，獨立 readback 109（tolerance 1），restore 100，manual/same session/no active motion。這是 readback 有界證據；optical angle 未量測。 | build24 已發布有界 zoom readback；未宣稱光學倍率或任意長 hold。 | 合法值、完成／取消、readback、fresh frame、restore 和 optical/physical 結果分開記錄。 | 在同一 firmware 下量一個短值的影像／視角變化，維持 raw/value 語義；不可把 110 寫成 1.1x。 |
| USB Roll | UVC 提供 signed raw roll range/step；不是自動直式或畫面旋轉。 | 有 Experimental Roll status/set/stop UI。`Sources/Pocket3BridgeApp/CameraRollControls.swift:4-36`。 | 有 raw range、readback 和 stop 狀態。`Sources/Pocket3Core/USBRoll.swift:39-86`。 | 有 setter，但 `authorize` 需要 `rollStopValidated`；正常 readback 不會自行設成 true。`CameraService.swift:1100-1130,724`。 | USB UVC。 | 0→1→0 exact readback 有紀錄；FE08/USB physical movement、stop validation 未通過。 | 明確 blocked／experimental；不可當已可用控制。 | v5 hardware validation 的全部 trials、physical stop、restore 和 error cleanup 都要 pass。 | 重新完成 development hardware validation，取得 physical motion/stop evidence 後才解除 gate。 |
| App 定義的 USB Home / Front / Back presets | 這些是本案依 UVC raw position 定義的 preset，不是 DJI 雙擊／三擊。 | 有 Center/Home、Front、Back 類按鈕；語義與 native preset 仍需分開。`Sources/Pocket3BridgeApp/GimbalRangeControls.swift:23-65`。 | 讀 default/min/max/step；`CameraService.swift:747-762`。 | 寫入 UVC absolute target；未證明機身語義。 | USB UVC。 | 尚無 current build 的每一 preset physical acceptance。 | UI/API 存在，未完成實機驗收。 | 每個 preset 要有明確定義、合法值、physical end state、stop、restore。 | 以 raw UVC preset 做三個獨立小測試；結果不能回填 native Center/Flip。 |
| DJI native Center / Flip / quick presets | 機身手勢存在；官方沒有 Pocket 3 host control API 可直接套用。 | 有 Center/Flip 類 native-looking controls，但 `wireless.nativeConnected` 才允許。沒有可用的 UI native connect。 | 可取得部分 BLE telemetry；沒有 native action completion readback。 | BLE/Wi-Fi native action source 存在；回覆 `physicalCompletionVerified=false`。`Sources/Pocket3Core/Pocket3Datalink.swift:230-355`。 | BLE candidate／Wi-Fi datalink；Wi-Fi 目前 disabled。 | BLE-only writes 通常 ignored；本案沒有 native physical pass。Kaze FE08 是外部 Pocket3 protocol baseline，不是本機證據。 | unavailable；不可把 USB preset 當 native。 | 原生 transport identity、命令 ACK、機身狀態／姿態、physical completion、cancel/restore 都要通過。 | 先完成明確身份和只讀共存，再做單一 native action；Wi-Fi 需隔離 Mac network。 |
| BLE scan、pairing、camera identity | Pocket 3 BLE service/characteristic 候選為 FFF0/FFF4/FFF5；候選 UUID 不等於身份。 | 有 Scan、選 peripheral、Connect Bluetooth、Pair camera。`Sources/Pocket3BridgeApp/WirelessGimbalConnectionView.swift:18-82`。 | 有 connection、session、notification、pair status。`Pocket3BluetoothDiscovery.swift:103-213`。 | 有 pair／cleanup／keepalive；預設 `pairOnly`，不等於 native control。 | CoreBluetooth BLE。 | 這台機器的 BLE link/pairing 和通知可用；identity state 仍是 `unverified_candidate`。 | experimental read telemetry；UI wording 正在 build25 修正，待 compile/live。 | 服務、韌體、裝置身份與 session 要有獨立確認；配對不得中斷 USB capture。 | 用 firmware/serial/model 可追溯的 identity probe；只讀通過後再啟用任何 write。 |
| BLE battery / charging / low battery | 機身有電量與充電狀態概念；USB data connection 不能推導 charging。 | 有 Bluetooth battery、charging、low/falling UI。`Sources/Pocket3BridgeApp/BluetoothBatteryView.swift:5-53`。 | 有 freshness、100%/notCharging、下降趨勢規則。`Sources/Pocket3Core/BluetoothBatteryMonitor.swift:31-138`。 | 無 product write。 | BLE telemetry。 | 實機讀到 100%／notCharging 和 fresh battery；真實 charging transition、低電量、外接供電未測。 | bounded read-only telemetry；不宣稱 USB charging。 | 多個同 session fresh samples、充放電轉換、過期回 unknown，並與機身畫面對照。 | 做一次充電與一次下降趨勢的只讀紀錄；分清 camera battery、handle、Mic battery。 |
| BLE pose | Pocket 3 回報 raw yaw/pitch/roll；未校準到 USB 座標。 | 有 Bluetooth Pose read-only view。`Sources/Pocket3BridgeApp/BluetoothPoseView.swift:5-27`。 | 有 deci-degree parser、freshness、bounds、04/05 frame admission。`Sources/Pocket3Core/BluetoothPoseObservation.swift:3-93`。 | 無已驗證 pose write。 | BLE telemetry。 | 實機 fresh pose readback；沒有 USB↔BLE calibration 或 movement correlation。 | read-only，bounded。 | 同時記錄 USB position、BLE pose、時間與相機姿態，建立校準和誤差界線。 | 一次靜態多姿態 correlation；不要用 raw yaw 冒稱角度已校準。 |
| BLE camera status / properties | 機身可回報私有 camera property；未知 code 必須保留 unknown。 | 有九類 read settings rows 與 camera/storage status；沒有 setter UI。 | 可解 lens、image effect、exposure、video、sensor orientation、photo、lapse、motionlapse、panorama，以及 `02/80`／`02/DC`；未知值保留 raw。 | Product UI 無 write；developer probes 分開。 | BLE DUML notifications。 | build25 paired session 收到九類 observation；單獨 query 只有前三類收到 push，其餘六類 timeout，但完整 UI sequence 後收到全部九類。未收到 matching ACK。`02/80`／`02/DC` 一致回報 SD 488015/176047 MiB、idle、remaining 36634 s；Webcam mode 保留 unknown `0x23`。 | 實機 read-only（有界）；readback 不代表 setting control，BLE/USB identity 仍未關聯。 | 每一欄有 source code、value domain、freshness、firmware compatibility；未知值不降級成 Auto。 | 對照機身切換 mode 後的唯讀值；setter 需另做 ACK、matching readback、physical result。 |
| BLE settings write：WB、focus mode、EV、color 等 | 機身支援部分設定；官方 host API 未提供。 | 目前只有 read-only settings UI；developer validation 不隨一般 UI 開放。 | 有 baseline/readback/ACK ledger。`Sources/Pocket3Core/CameraSettingsCommand.swift:12-83`、`CameraSettingsState.swift:55-159`。 | 有 single-command writer、0.8 s ACK + 3 s readback fence；無 retry/restore。`BluetoothCameraSettingWriter.swift:133-313`。 | BLE DUML FFF5。 | WB 5600K 寫入結果 7/6 reports Auto、無 ACK/matching readback、`applied=false`；其餘 setter 未通過。 | unfinished；不可顯示可用寫入。 | 每個 property 要有 accepted、ACK、matching readback、實際畫面／模式兼容和失敗 cleanup。 | 先選一個 read-only 已知 property 做 no-op/controlled write，得到 protocol-level evidence 後才擴張。 |
| BLE tap focus / focus readback | Pocket 3 有機身對焦；Webcam AVFoundation capability 另計。 | Preview click/focus UI 只在 `canFocus` 時出現；marker 不證明光學合焦。`Sources/Pocket3BridgeApp/CameraFocusModel.swift:7-109`、`CameraFocusPreview.swift:41-92`。 | 有 UVC/AVFoundation focus capability 和 BLE lens observation。 | AVFoundation focus point 可提交；BLE tap-focus 是 developer probe，最多四個 exact writes。`BluetoothTapFocusProbe.swift:117-313`。 | USB AVFoundation 或 BLE DUML。 | BLE tap focus 無 ACK／無 optical confirmation；未完成 product write。 | unfinished。 | focus mode、point、ACK/readback、影像 sharpness/機身 lens state 和 cancel 都要一致。 | 只讀確認 capability 與 lens state；不要把 VLM ROI 或 marker 送成 focus success。 |
| BLE FE08 / FE09 gimbal commands | Kaze revision `341a35de...` 是 Pocket3 protocol baseline；FE08 physical-confirmed、FE09 strongly identified 是 Kaze 作者證據，並非本案實機結果。 | 無一般 product native gimbal control UI。 | 有 BLE frame/telemetry admission、developer probe。`Pocket3BluetoothDiscovery.swift:276-368,859-920`。 | 有單次 FE08/native probe，無 retry；BLE-only gimbal pulse 通常被忽略。 | BLE DUML。 | 本案 FE08 full-pairing 實測：一次提交、3 s/27 poses 不變、無 ACK；cleanup pass。FE09 無獨立成功。 | protocol research／blocked。 | 在這台 firmware 取得命令 ACK、姿態變化、physical completion、neutral restore，且不影響 USB。 | 先重做 read-only identity/pose，再只測一個 FE08；不要把 Kaze physical-confirmed 移植為本案 pass。 |
| 機內錄影 start/stop、recording state | 機身支援；`02/02` write、`02/80` state 是私有 protocol 候選。 | build24 無已驗證 body record button／status UI；build25 `02/80` read-only UI 待 compile/live。 | source 已有 `02/80` readback WIP；歷史 Kaze readback 只作候選。`Sources/Pocket3Core/Pocket3CameraStatusReadback.swift`。 | 尚無本案 product writer；不把 capture session 當機內 recording。 | BLE/Wi-Fi 私有 transport 未完成。 | 沒有 body recording command 的本案 ACK、SD output 或 state transition evidence。 | unavailable；build25 pending。 | ACK 後必須看到 `recording` state transition、SD/remaining time、實際檔案和 stop/timeout cleanup。 | 先只讀 `02/80` 對照機身錄影畫面；不要提交 `02/02`。 |
| 機內單張拍照、JPEG/RAW、倒數、照片比例 | 機身／Mimo 可用；USB JPEG frame 不是機內 still photo。 | 無已驗證 body photo UI。 | 只有候選 `shootPhoto`/`photoFormat`/`photoCountdown`/`photoFrame` source，未在本機讀通。 | 無本案 product writer。 | BLE/Wi-Fi 私有 transport 未完成。 | 無本案 ACK、SD JPEG/RAW、countdown 或 aspect evidence。 | unavailable。 | 實際 shutter、檔案、格式、倒數、相機狀態和失敗回讀全通過。 | 先做 read-only mode/property inventory，再決定是否做 photo writer。 |
| 機內 video/photo mode、Pro、ISO、shutter、EV、WB、AF、color | 機身選單和 Mimo 提供部分設定；Webcam UVC 沒有這些標準 controls。 | 有 read-only BLE settings rows；沒有可承諾的完整 settings editor。 | build25可讀九類property；video compression已映射成typed H.264/HEVC baseline。 | developer writer新增`02/AB` H.264/HEVC單次命令，要求完整videoParameters baseline與ACK+matching readback；一般UI仍不開放。 | BLE DUML candidate；Wi-Fi disabled。 | 九類property read已通過；HEVC目前只讀到機內設定，writer尚未真機驗收。USB descriptor沒有HEVC format。 | read-only通過；camera-side HEVC writer source/test only。 | 每一 mode/setting 的合法 domain、setter ACK、matching readback、影像結果與跨模式限制都要記錄。 | 先完成只讀 mode/property schema，避免用 OpenPocketCine 或其他型號 opcode。 |
| Timelapse / Hyperlapse / Motionlapse | 機身/Mimo 功能候選；官方 host 路徑未提供。 | 無已驗證控制 UI。 | 候選設定 source，沒有本機實機 state。 | 無已驗證 writer。 | 私有 BLE/Wi-Fi 未完成。 | unknown；不以普通錄影或 USB stop 推論。 | unavailable。 | 有合法間隔/時長、路徑點、preview/stop、實際檔案和 mode readback。 | 先標示 camera-side only；等獨立 Pocket3 command/readback evidence。 |
| Panorama、Low Light、1080p8x、4K4x、SpinShot、MedTele | 其中部分是 Pocket 3 機身/Mimo 功能；官方沒有 Pocket3 host API。官方支援資料也不把這些列為 Webcam host controls。 | 無可承諾的 host UI。 | 無本案 host readback。 | 無本案 host writer。 | USB Webcam 不承接這些機內模式；BLE/Wi-Fi 未完成。 | 沒有本案 host 實機 evidence；Kaze/OpenPocketCine 候選不能代替。 | explicitly unsupported for current host claim／unknown for body internals。 | release 不可用。 | 必須先有 DJI 官方 Pocket3 host API 或本 firmware 的獨立 command、readback、physical output；否則維持限制。 | 不 probe 未知 opcode；只更新官方文件與機身/Mimo 操作指引。 |
| ActiveTrack、Face Auto-Detect、Dynamic Framing | ActiveTrack 可在機身／Mimo 使用；Webcam 只有 2024 release note 的直播線索。沒有官方 Pocket3 host API。 | App 有 local observation／AI 入口，但不應標成 DJI ActiveTrack。 | 可讀 USB frame，Vision/CoreAI 可做本機分析；沒有 DJI tracking state readback。 | assistFraming 只允許受限 zoom；不取得 camera tracking control。 | USB UVC + local AI；native tracking transport unknown。 | build23 observe/assist 有界 gate pass；沒有 ActiveTrack/Face/Dynamic Framing host physical evidence。 | local observation bounded；native feature unavailable。 | 要有 Pocket3-specific start/stop/target command、camera state、physical tracking 和 conflict policy。 | 將 UI 名稱保持 local observation；先取得官方或 firmware-specific host evidence。 |
| App 本機 image/local video、frame/time、ROI、marker、export | 不使用相機控制；選定檔案或實際 frame 才送模型。 | 有 open image/local video、time slider、±1 s menu、ROI drag、Whole frame reset、模型選擇、JSON/Markdown export。 | `ImportedImagePreview.swift`、`ImportedVideoSource.swift` 讀 basename、actual PTS、orientation、frame/ROI。 | 只寫 immutable result export；包含 basename/question/model/frame/ROI/time，不含 media bytes/path/action claims。seek/region change 清舊結果。 | 本機檔案／AVAsset；offline。 | `artifacts/offline-media-build23/final/verification-gate.json`、`artifacts/media-workspace-gate-1789044947/result.json`、`artifacts/media-workspace/build23-smoke-6/result.json` 均 PASS；只用 synthetic image/video，MLX count 有 uncertain warning。 | build24 verified media workspace。 | real-world media 可另行驗證，但 export/privacy contract 必須保持 immutable、可追溯、無 bytes/path。 | 只增加獨立 real media cases；不把 synthetic OCR/count 當 camera accuracy。 |
| Apple / MLX observe、assistFraming、task routing | 模型不改 Pocket3 機身能力；Apple/MLX 是本機 observation roles。 | 有 Ask、Count、Locate、OCR、observe/assistFraming、cancel、privacy pause。 | 有 fresh frame、model result、action ledger、max-call/lease checks。`Sources/Pocket3Intelligence/ObservationExecutionRoles.swift:5-31`、`ObservationTools.swift:37-249`。 | observe 無 camera action；assistFraming 只可 bounded UVC zoom，不能繼承 arbitrary movement。 | local AI + USB capture/MCP。 | build23 synthetic media gate PASS；MLX real hardware bounded zoom PASS；Apple observe safety guardrail、cleanup PASS；camera access/session 未被 offline task 改變。 | verified bounded workflows；composition/tracking/events/multi-frame compare 仍 pending。 | source/session fence、fresh frame、bounded action、cancel/cleanup、無 action claims 都需逐任務證明。 | build25 `remoteTask` fence compile/live 後再重做 source switch while task；不要先標 pass。 |
| CoreAI detection / compute device | CoreAI YOLOS/MLX local analysis 可執行；結果不是 DJI camera action。 | 有 CoreAI diagnostics/detection。 | 有 typed count/point/absence；executionDevice 只表示選擇／fallback，未證明實際 ANE/GPU。`Sources/Pocket3Intelligence/PerceptionEngine.swift:28-76`。 | 無 camera write。 | 本機模型／GPU/CPU fallback。 | copied-App gate 有 inference/memory release；沒有 Instruments 實測的 compute unit 證據。 | experimental local AI。 | 以 Instruments/可追溯 runtime evidence 證明實際 device，再固定 performance/cost baseline。 | 做一次獨立 Instruments run；維持 `executionDevice` 的不確定語義。 |
| Audio capture、PCM、長測 | Pocket 3 Webcam 可提供音訊；機內 audio setting 另需私有 transport。 | 有 audio diagnostic/validation、permission、enable/disable UI。 | 有 sample rate/channels/PCM stats、stream validation。`Sources/Pocket3Core/AudioTestPolicy.swift:3-11`、`StreamValidation.swift:16-68`。 | 可在 validation session enable/disable capture audio；不是寫機內增益。 | USB AVFoundation。 | 歷史實機有 stereo 48 kHz；build23 offline media gate 不碰 camera；長測／模式變化仍 pending。 | bounded audio baseline；未完成 full audio acceptance。 | 當前 firmware 同時有 audio frames、正確 PCM、無掉幀、stop/permission cleanup 和長測 report。 | 先做短 current-device capture，再決定長測；不把機內 Mic 增益當可調 setter。 |
| Files / media transfer / playback | 機身有 SD media；Mimo/官方流程可用。 | 無已驗證的 camera file browser/download/delete/format UI；local import 是 Mac file。 | build25 `02/DC` storage read-only path 待 compile/live；沒有可靠 file list transport。 | 無 delete/format/download writer。 | BLE/Wi-Fi/private media transport 未完成；USB Webcam 是 capture path。 | 無本案 SD list/file transfer/delete physical evidence。 | unavailable；storage read-only pending。 | 讀取 total/free、檔案身份、下載校驗、取消、空間和 destructive confirmation 全通過。 | 先只讀 `02/DC` 對照 SD；不提交 delete/format。 |
| DJI Mic 2 / Mic Mini、廣角/ND/黑柔鏡、Battery Handle 等 accessories | 官方配件依 firmware/相機模式有不同支援；不能把主機 telemetry 套給 TX 或 handle。 | 無完整 accessory manager。 | 無各配件獨立 capability/readback。 | 無 accessory writer。 | USB/BLE/private accessory paths unknown。 | 無本案 accessory pairing、gain、noise reduction、battery、backup evidence。 | unavailable／unknown。 | 每一配件建立獨立身份、電量、設定、版本和 session；與 camera capture 共存要實測。 | 先列舉裝置身份與官方相容性；不增加猜測按鈕。 |
| USB power、charging、電流 | USB descriptor/IOKit 可表示配置／可用電力；不是電池 charging sensor。 | 有 diagnostics 顯示 connection/power facts；不應顯示虛構 current/charging。 | 有 USB attachment/configuration properties。`Sources/Pocket3Core/USBPower.swift:4-118`。 | read-only；沒有寫 power allocation 或 charging mode。 | USB/IOKit。 | offline/hardware gate 只能證明 USB capture；沒有 measured current、PD negotiation 或 battery charging transition。 | read-only facts；charging `unknown`。 | 外接 power、實測電流、battery charging 狀態須來自相容 source，並有 stale/unknown 規則。 | 只收集 matching attachment 的 read-only properties；不以 `bMaxPower` 推算耗電。 |
| USB/BLE/Wi-Fi access、privacy、source switching | App 有 manual/observe/control access 與 privacy pause；Wi-Fi join 會改變 Mac network。 | 有 source/access/permission/pause UI。`Sources/Pocket3BridgeApp/Pocket3BridgeApp.swift:270-303,705-772`。 | 有 camera status/session/access、freshness 和 local task state。 | manual/mcp control 有 ownership/lease；source switch 的 baseline fence 尚未涵蓋 remote task。 | USB UVC、BLE read-only；Wi-Fi disabled。 | offline gate camera frames/session/access unchanged；build23 hardware cleanup pass。 | public bounded access；build25 fence pending。 | 切換 source 前必須等待／取消 UI、MCP、remote observation；舊 task 不能碰新 source/session。 | 編譯並 live 測 `remoteTask` fence；在此以前不要同時切換 source 和外部 assistFraming。 |
| Wi-Fi datalink、native joystick、camera network | Kaze 描述 static host `192.168.2.1` TCP/UDP datalink；本案沒有安全 network switching。 | 沒有 Join Wi-Fi／native connect 的可用 product button。 | source 有 telemetry/status/heartbeat/neutral action schema。`Sources/Pocket3Core/Pocket3Datalink.swift:39-225`。 | validation 路徑明確拋 `wifi_control_disabled`。`Sources/Pocket3BridgeApp/WirelessValidation.swift:46-48`。 | Wi-Fi TCP/UDP，現在 disabled。 | no Wi-Fi physical evidence；Mac network policy intentionally prevents join。 | disabled。 | 隔離網路、連線身份、TCP/UDP handshake/window、ACK、telemetry、neutral、restore 和 network recovery 全通過。 | 取得明確 network isolation authorization 後再做單次 read-only handshake；未授權不 probe。 |
| MCP / CLI | 公開 App 提供本機 MCP／CLI；native wireless 不在工具承諾內。 | App server/diagnostics UI；CLI 可獨立呼叫。 | `camera_status`、`capture_frame`、`camera_zoom_status` 讀取；使用 session/access/fresh frame。`Sources/Pocket3Core/MCPCameraToolContract.swift:3-91`、`Pocket3CLI.swift:287-310`。 | `camera_set_zoom`、`move_gimbal`、`stop_gimbal` 有 bounded USB write；native action unavailable。 | local Unix IPC，最多 client/work-slot 規則；USB service。`Sources/Pocket3Core/IPC.swift:60-145`。 | release/offline tool gates PASS；real hardware only bounded zoom；沒有 general physical pan/tilt/native proof。 | build24 shipped；build25已修正`camera_status`語義並加入remote-task source-switch fence，完整Release tests通過，MCP source-switch live concurrency仍待驗。 | 每個工具回傳 accepted/completed/verified、session、transport、physical boundary；取消和 source switch 不得跨 session。 | 做一個MCP remote observation執行中切source／取消／session不變的live probe。 |
| Sparkle update、ZIP/DMG、signature | 這是 App 發布能力，不是 Pocket3 device control。 | 有 update permission/status UI。 | 有 exact source、hash、Ed25519 feed/signature verification。`Sources/Pocket3BridgeApp/AppUpdateController.swift:58-209`。 | 可下載／安裝已簽名 release；不改 camera firmware。 | HTTPS/Sparkle/GitHub。 | build24 beta1→candidate update、公開 hash、feed、ZIP/DMG/signatures PASS；Developer ID/notarization 未完成。 | release verified；與 firmware update 分開。 | 發布 artifact、feed、hash、signature、update install 和 rollback/permission 都保持可追溯。 | 保持目前 release checks；不要把 Sparkle 當 camera firmware updater。 |

## build25 明確 pending 項目

以下是目前工作樹已開始但尚未 compile/live 的三組工作；它們不改變上表任何 release 狀態：

1. `Sources/Pocket3Core/Pocket3CameraStatusReadback.swift` 與 `Sources/Pocket3BridgeApp/BluetoothCameraStatusView.swift`：`02/80` recording/camera status、`02/DC` storage 的 **read-only** schema/UI；要等編譯、真機通知、session/freshness 和機身畫面對照。
2. `Sources/Pocket3BridgeApp/ImageObservationSource.swift`：source switch 加入 remote observation/task fence；要等 UI、MCP、assistFraming 競態 live probe 和舊 task cleanup。
3. `Sources/Pocket3BridgeApp/WirelessGimbalConnectionView.swift`、`WirelessGimbalModel.swift` 與 identity 狀態：把「Camera paired」等字樣改成反映 unverified identity；要等 compile/live BLE identity evidence。

## P0 / P1 目前阻塞與最小下一步

### P0

- **Source switch 的 remote-task fence 已加入、仍待完整 App gate。** `ImageObservationSource.canChangeObservationSource` 現在要求 `remoteTaskCount == 0`，focused test 已覆蓋外部工作執行中拒絕切換與完成後恢復；完整 Release/App gate 尚未跑完。

### P1

- **Native BLE/Wi-Fi 控制路徑沒有可用 product path。** `WirelessGimbalConnectionView.swift:18-82` 沒有 native connect/join button；`WirelessGimbalModel.swift:87-205` 有 model path 但預設 pair-only；`WirelessValidation.swift:46-48` 對 Wi-Fi control 明確回 `wifi_control_disabled`。最小下一步是只讀身份／telemetry，再隔離網路做單一 action。
- **Roll 永久卡在 stop validation gate。** `USBRoll.swift:39-86` 要求 `rollStopValidated`；`CameraService.swift:724` 的一般 readback 不會將它設為 true。最小下一步是完整 v5 physical validation；在此以前 Roll 保持 experimental blocked。
- **UVC presets 與 native-looking controls 語義重複。** `GimbalRangeControls.swift:23-65` 是 App 定義 raw absolute position；`Pocket3BridgeApp.swift:757-765` 另有 Center/Flip 外觀。最小下一步是 UI 及 tool contract 明確寫出 USB raw preset 與 DJI native action 的差別，並各自驗證。
- **USB slider 與 presets 的 capability gate 已分開。** `GimbalRangeControls.swift` 只有 preset HStack 在缺少 `defaultPosition` 時停用；pan/tilt slider 使用 writable/range gate。仍需實機確認缺少 default 的裝置狀態與 movement fence。
- **BLE identity wording 不能把配對說成已確認 Pocket 3。** `Pocket3BluetoothDiscovery.swift:76-100` 保留 `unverified_candidate`；UI wording 由 build25 修正中。最小下一步是 firmware/model identity probe，再把 wording 和 evidence level 綁定。
- **UVC status read 已區分 transient control failure 與 attachment invalidation，待完整 gate／真機斷線驗證。** build25 source 在 capture 仍 fresh 且 identity 未失效時保留 session/capabilities並回報 degraded control read；identity mismatch、裝置消失或 stale capture 仍失效。下一步是完整測試及真機拔插／恢復對照。
- **MCP status 文案已改成精確描述。** `camera_status` 不啟動 AVFoundation capture；capture 已活動時可能讀 retained UVC control session，並把 control-read degradation 與 video stream 分開回報。待完整 MCP gate。

## 外部與歷史來源

### 官方 Pocket 3 資料

- [DJI Osmo Pocket 3 specifications](https://www.dji.com/osmo-pocket-3/specs)：機身角度、錄影／照片／配件等官方能力；不代表 host API。
- [DJI Osmo Pocket 3 FAQ](https://www.dji.com/osmo-pocket-3/faq)：Webcam、ActiveTrack、供電與相機使用限制。
- [DJI Osmo Pocket 3 support](https://www.dji.com/support/product/osmo-pocket-3)：官方支援與更新入口。
- [DJI Osmo Pocket 3 User Manual](https://dl.djicdn.com/downloads/DJI_Osmo_Pocket_3/UM/20250826/DJI_Osmo_Pocket_3_User_Manual_v1.0_en.pdf)：機身／Mimo 操作語義，不是本案 host command specification。
- [DJI Osmo Pocket 3 release notes](https://dl.djicdn.com/downloads/DJI_Osmo_Pocket_3/RN/20250826/DJI_Osmo_Pocket_3_Release_Notes_en.pdf)：官方 firmware release context；本案裝置版本由操作者在機身確認為 `01.06.10.04`。
- [DJI Webcam support article](https://repair.dji.com/help/content?customId=zh-cn03400006962&lang=zh-CN&re=CN&spaceId=34)：Webcam 支援與 2024 live-stream 線索；不提供 Pocket 3 host control SDK。
- [DJI Osmo GPS Controller Demo](https://github.com/dji-sdk/Osmo-GPS-Controller-Demo)：官方 demo 列出其他產品，沒有 Pocket 3；不能把其 camera-status、battery 或 record schema 移植到本案。

### 固定 revision 的開源研究（候選，不是本案實機通過）

- [Kaze-for-DJI revision `341a35de18493ff61f97c93b8b10161a7512aa36`](https://github.com/brianmerchant/Kaze-for-DJI/tree/341a35de18493ff61f97c93b8b10161a7512aa36)：Pocket 3 protocol baseline；[camera settings](https://github.com/brianmerchant/Kaze-for-DJI/blob/341a35de18493ff61f97c93b8b10161a7512aa36/ios/Pocket3Controller/Pocket3CameraSettings.swift)、[camera readback](https://github.com/brianmerchant/Kaze-for-DJI/blob/341a35de18493ff61f97c93b8b10161a7512aa36/ios/Pocket3Controller/Pocket3CameraReadback.swift)、[telemetry](https://github.com/brianmerchant/Kaze-for-DJI/blob/341a35de18493ff61f97c93b8b10161a7512aa36/ios/Pocket3Controller/Pocket3TelemetryDomain.swift)、[gimbal commands](https://github.com/brianmerchant/Kaze-for-DJI/blob/341a35de18493ff61f97c93b8b10161a7512aa36/android/app/src/main/java/com/pocket3/gimbaltest/Pocket3GimbalCommands.kt)、[DumlTransport](https://github.com/brianmerchant/Kaze-for-DJI/blob/341a35de18493ff61f97c93b8b10161a7512aa36/ios/Pocket3Controller/DumlTransport.swift)、[protocol notes](https://github.com/brianmerchant/Kaze-for-DJI/blob/341a35de18493ff61f97c93b8b10161a7512aa36/docs/POCKET3_DUML_PROTOCOL.md)。FE08 在該來源標為 physical-confirmed、FE09 為 strongly identified；兩者都不能代替本機實測。
- [OpenPocketCine revision `40a89d5a77f91d243300cab717e29524e54b95e2`](https://github.com/erik-sutton95/OpenPocketCine/tree/40a89d5a77f91d243300cab717e29524e54b95e2)：A6/A5/89 tracking 與 tap-focus 的目前 live proof 是 Pocket 4／4 Pro／Nano，不是 Pocket 3；本案只被動記錄符合嚴格 envelope 的候選 frame，不送出命令。
- [lib-osmo-ble revision `021e96c2bec7e9a2a81296292545bc1ee432af49`](https://github.com/yigitkonur/lib-osmo-ble/tree/021e96c2bec7e9a2a81296292545bc1ee432af49)：可作協議研究；其 BLE-only gimbal path 通常被 Pocket 3 忽略，不能當 motor support。

### 歷史 snapshot（不覆蓋本表）

以下文件保留原始日期、規劃或舊驗收條件；它們不是 current state source，也沒有在本輪改寫：

- [`PROJECT_PLAN.md`](../PROJECT_PLAN.md) — historical planning snapshot。
- [`BUILD_PROPOSAL.md`](../BUILD_PROPOSAL.md) — historical proposal snapshot；早期四工具／CLI 描述不等於現在的六個 MCP tools。
- [`DEVICE_CAPABILITY_ROADMAP.md`](DEVICE_CAPABILITY_ROADMAP.md) — historical capability snapshot；其中 P0–P3 和「待 transport」仍是研究脈絡，不能把研究路徑當已完成。
- [`HARDWARE_ACCEPTANCE.md`](HARDWARE_ACCEPTANCE.md) — dated historical physical trials；build13–15 的結果不能自動套用到 build24 或 build25。

本表新增或更新一項功能時，必須同時填寫 transport、read/write、實機 evidence、completion criteria 和下一 probe；缺一項就保留 `unknown` 或相應的 blocked 狀態。
