# Pocket 3 實機探測紀錄

日期：2026-09-07  
狀態：**USB 影音與小幅視角控制已有正面證據；里程碑 0 尚未完整通過。**

使用者已連接相機並授權輕量探測。本次使用 AVFoundation、IOKit／uvc-util；沒有切換 Wi-Fi、更新韌體、寫入機內錄影、測試大角度運動或進行耐久測試。

## 1. 環境與裝置

| 項目 | 本次記錄 |
|---|---|
| 主機 | Apple Silicon，arm64 |
| 作業系統 | macOS 27.0，build 26A5425a |
| USB 裝置名稱 | `DJIPocket3` |
| AVFoundation 名稱 | `OsmoPocket3` |
| VID／PID | `0x2ca3`／`0x0023` |
| USB location | 由當次 USB 枚舉取得（不固定連接埠） |
| 相機 unique ID | `0x11000002ca30023` |
| USB 連線速率 | 480 Mb/s，IORegistry 與使用者資料一致 |
| USB 產品版本 | `bcdDevice=0x0504`；**不是已核實的相機韌體版號** |
| 相機韌體 | 未從相機／Mimo 讀取，未知 |
| 配置電力 | 使用者提供 500 mA／2.5 W；未量測實際供電或耗電 |
| UVC 版本 | uvc-util 顯示 1.00 |
| 控制工具 | jtfrey/uvc-util，commit `8110da7025c95eea3096a7181af9a46c0cc7ac37`，本機 clang 編譯 |

序號已用於本地裝置匹配，但其形式不足以證明全球唯一。日後多機支援須另外驗證，不能只靠 VID/PID 或序號。

## 2. 影像：枚舉與實際輸出不同

AVFoundation 列出 720×1280、1080×1920、1280×720、1920×1080、3840×2160；每種含 `420v`／`2vuy` 格式。大部分列 25／約 30fps，部分也列約 24fps；4K 的 `2vuy` 還列出約 48／50／60fps。

這些是主機 API 的格式描述，**不能直接推定 USB 線路傳送相同的未壓縮格式，或所有組合都能輸出**。原始枚舉見 [AVFoundation 記錄](research/2026-09-07/avfoundation-inventory.json)。

| 實際請求 | 測試時間 | 收到影格 | 實際尺寸 | 排除前 1 秒後的 PTS fps | 判定 |
|---|---:|---:|---|---:|---|
| 1080p30 | 約 5 秒 | 144 | 1920×1080 | 29.96 | 短測成功 |
| 4K30 | 約 5 秒 | 145 | 3840×2160 | 29.99 | 短測成功 |
| 4K60 | 約 5 秒 | 0 | 無 | 無法計算 | 此配置未通過 |
| 1080p30＋音訊＋pan 操作 | 約 9 秒 | 250 | 1920×1080 | 30.00 | 並行短測成功 |
| 1080p30＋音訊＋tilt 操作 | 約 9 秒 | 275 | 1920×1080 | 30.00 | 並行短測成功 |

測試時間是啟動擷取後的等待時間；包含主機啟動等待的程序耗時稍長。fps 計算為有效期間內 `(影格數−1)/(最後 PTS−最初 PTS)`，以主機 callback 第一筆後的一秒排除啟動段。這不是曝光時間、端到端延遲或長期穩定度測量。沒有將 4K60 的一次失敗推論成所有韌體／格式都永遠不支持。

可查閱 [1080p30 數據](research/2026-09-07/usb-1080p30.json)、[4K30 數據](research/2026-09-07/usb-4k30.json)、[4K60 數據](research/2026-09-07/usb-4k60.json)與 [4K 實際影格](research/2026-09-07/usb-4k30-last.jpg)。PTS 與主機時間保留於 JSON，可重算結果。

## 3. 音訊：已收到相機 USB 路徑的樣本

選定裝置為 `AppleUSBAudioEngine:DJI:DJIPocket3:…:4`，沒有使用系統預設麥克風。枚舉為 **48,000 Hz、16-bit、雙聲道 LPCM**。

兩次影音並行測試各收到 847 個 buffer、433,664 個 stereo sample frames。pan 測試兩聲道 RMS 約為 112.93／105.61、peak 為 627／610；tilt 測試 RMS 約為 68.13／63.69、peak 為 560／510，皆為 signed 16-bit PCM 的原始幅度單位。

結論是輸入資料有非零訊號，且影音與控制可以在本次短測並行。**沒有保存原始音訊、做語音聽辨、證明訊號一定來自機身而非已配對發射器，或量測影音同步。** 雙聲道也不等於三支麥克風的獨立原始資料。

數據：[pan 影音紀錄](research/2026-09-07/usb-pan-probe.json)、[tilt 影音紀錄](research/2026-09-07/usb-tilt-probe.json)。

## 4. UVC 控制：小幅 pan／tilt 寫入造成可見視角變化

| 控制項 | 最小 | 最大 | 宣告步進 | 初始讀回 |
|---|---|---|---|---|
| `pan-tilt-abs.pan` | −126000 | 774000 | 3600 | 0 |
| `pan-tilt-abs.tilt` | −324000 | 324000 | 3600 | −32400 |
| `zoom-abs` | 100 | 400 | 1 | 100 |
| `roll-abs` | −30 | 30 | 1 | 0 |

全表都是工具原始值，不是本專案校準後的角度／倍率。只測了 pan／tilt；zoom 和 roll 僅查詢。本機控制枚舉沒有列出 relative pan／tilt 或獨立 stop。一次額外 relative 查詢被工具拒絕，之後只查已宣告項目。[完整控制枚舉](research/2026-09-07/uvc-controls-complete.txt)。

**操作方式：** 在持續取像＋音訊下，讀取原值；約兩秒後只將一軸增加 3600；等待約兩秒，再寫回該次測試的原值。兩軸分開測試，沒有送出速度型持續運動或猜測的回中封包。

| 實驗 | 請求目標 | 約 0.7 秒後讀回 | 寫回原設定後讀回 |
|---|---|---|---|
| pan | pan=3600、tilt=−32400 | pan=3960、tilt=−32400 | pan=−360、tilt=−32400 |
| tilt | pan=−360、tilt=−28800 | pan=0、tilt=−28800 | pan=0、tilt=−32400 |

最後另行查詢：**pan=0、tilt=−32400、zoom=100、roll=0**，與整輪最初讀回一致。程式的 `restored=true` 表示恢復命令成功送出；實際回讀與圖片仍需分開看。[pan 命令紀錄](research/2026-09-07/usb-pan-probe-commands.json)、[tilt 命令紀錄](research/2026-09-07/usb-tilt-probe-commands.json)。

### 前後畫面證據

| 實驗 | 動作前 | 小幅移動後 | 恢復後 |
|---|---|---|---|
| pan | [影格 2](research/2026-09-07/usb-pan-probe-sample-2.jpg) | [影格 6](research/2026-09-07/usb-pan-probe-sample-6.jpg) | [影格 12](research/2026-09-07/usb-pan-probe-sample-12.jpg) |
| tilt | [影格 2](research/2026-09-07/usb-tilt-probe-sample-2.jpg) | [影格 6](research/2026-09-07/usb-tilt-probe-sample-6.jpg) | [影格 12](research/2026-09-07/usb-tilt-probe-sample-12.jpg) |

已目視檢查動作前／後的牆面特徵變化。使用 Apple Vision 平移配準，1920×1080 圖片的配準量如下：

| 比較 | pan 實驗 `(tx,ty)` | tilt 實驗 `(tx,ty)` |
|---|---|---|
| 動作前對移動後 | `(−20,−1)` px | `(−16,−22)` px |
| 動作前對恢復後 | `(3,−1)` px | `(−7,0)` px |
| 恢復後兩張晚期影格 | `(0,0)` px | `(0,0)` px |

配準是影像分析估計，符號屬配準座標，不能直接當雲台方向或物理角度。tilt 測試也有水平位移，說明需要進一步量測軸間影響／機身穩定性。零配準量只表示這兩張圖在方法解析度下沒有平移；不是永久靜止或緊急停止證明。沒有另外拍攝機身運動或使用角度量具。[配準數據](research/2026-09-07/frame-registration.json)、[Apple Vision 方法](https://developer.apple.com/documentation/vision/vntranslationalimageregistrationrequest)。

## 5. 對實作的直接影響

1. **優先建立 USB 後端。** AVFoundation 取像／音訊＋IOKit UVC 控制已在此主機短測成立，Wi-Fi 暫時保留為備案。
2. **可以研究絕對目標模式，但加入容許誤差。** 回讀可能不同於設定值，不應要求整數完全相等才成功；容許範圍要靠重複測試決定。
3. **停止仍是缺口。** 小幅定位自然結束不等於可以在途中打斷，更不等於拔線或殺程序會可靠停止；M0 尚未完成。
4. **1080p30 作首版基線，4K30 作已短測的選項。** 4K60 先標示未通過，不因格式清單列出就對外宣稱支持。
5. **音訊能力可早於原計劃取得證據。** 已有實際 PCM，但語音、來源隔離及同步仍是獨立工作。
6. **不根據 480 Mb/s 或 2.5 W 推斷耐久。** 目前沒有測量線路實際碼率、供電餘量、電量趨勢、熱穩定或兩小時以上運作。

## 6. 重現方式與剩餘測試

探測程式與資料位於 [research/2026-09-07](research/2026-09-07/README.md)。它們是研究用程式，不是正式相機核心。讀取裝置和控制能力可重現；動作腳本會實際轉向，只應在相機架設穩定、正確裝置選定時手動執行。

下一輪需要：韌體記錄、30 分鐘影音、每軸多次小幅往返、真正中途停止、取消與程序中止、USB 斷開／重連、休眠恢復、端到端延遲及音訊同步。此次沒有執行上述項目，不能把本紀錄當完整產品驗收。
