# Pocket 3 Controller — 專案計劃與研究基線

建立日期：2026-09-07  
目前狀態（2026-09-09）：App、相機核心、MCP／CLI 已實作；0.0.1 beta 1 已發布，beta 2 持續開發。最新完成項目與限制以 [TODO](TODO.md) 及 [硬體驗收](docs/HARDWARE_ACCEPTANCE.md) 為準；下方初期技術里程碑保留為研究基線。

研究報告：[DEEP_RESEARCH.md](DEEP_RESEARCH.md)  
實機紀錄：[HARDWARE_VALIDATION.md](HARDWARE_VALIDATION.md)

產品形態與構建提案：[BUILD_PROPOSAL.md](BUILD_PROPOSAL.md)。已採用 macOS 27 原生 AI 觀察 App＋附帶 MCP／CLI，納入 Foundation Models／MLX／Core AI；後續擴充範圍見 [裝置功能路線圖](docs/DEVICE_CAPABILITY_ROADMAP.md)。

**2026-09-07 研究後決策**

- USB 已在此台 Mac 成功取得 1080p／4K 約 30fps 影像；1080p 取像、雙聲道 USB 音訊與小幅 pan／tilt 控制可並行。第一版優先 USB。
- 格式清單雖列出部分 4K60，本次五秒測試為零影格；不可當成支持能力。
- pan／tilt 寫入有回讀與前後畫面位移證據，但存在回讀差值及返回殘差；中途停止、斷線和重複性仍需驗證。
- 差異化定位為「可驗證的主動觀察」：動作後取新畫面、可靠失敗回報、觀察區域和同視角比較。通用取像／PTZ MCP 已有相關專案。
- Wi-Fi 主要協議參考改為 Kaze for DJI；舊 Python 專案保留為 macOS 原型對照，不能直接混用不同專案的雲台封包。

**一句話定位**

讓 AI 透過 DJI Osmo Pocket 3 看見現場，並控制相機轉向需要觀察的地方。優先驗證 macOS，提供可獨立使用的相機控制核心、MCP 接口，以及最小的 LLM 示範。

**命名與專案邊界**

- 產品名稱：**Pocket 3 Controller**。
- 本地目錄：`~/Documents/YuhuanStudio/Pocket3-Controller`。
- 遠端 repository：`YuhuanStudio/Pocket3-Controller`。
- MCP 為 App 的接入功能之一；另提供 CLI、相機核心及內建模型工具流程。
- 這是非官方整合專案，與 DJI 無隸屬或背書關係。
- Git、遠端 repository、MIT 授權及 beta 1 發布均已建立；公開版本與開發版本分開記錄。

**為什麼獨立做**

過去常把小範圍創新放進大專案，導致能獨立驗證與展示的成果，必須等待整個系統成熟才被看見。

這次把成果單位縮小到「相機接入 AI」：它必須能獨立安裝、測試、展示和發布。Yunmo 是未來的使用者之一，不是本專案的執行依賴，也不是發布前置條件。

第一版的價值是可重現的觀察與動作閉環：提出任務 → 擷取畫面 → 決定是否轉向 → 執行動作 → 再取畫面確認。相機控制、MCP 和示範應各自保持小而明確的責任。

**已知證據與仍待驗證的能力**

以下整合 2026-09-07 官方文件、開源原始碼及本機短測。實際數據和限制見硬體紀錄；幾秒鐘的成功不代表可靠性驗收。

| 能力 | 初步證據 | 本專案需要驗證 |
|---|---|---|
| USB 影像輸入 | 官方韌體加入 4K UVC；本機 1080p30 與 4K30 短測成功 | 30 分鐘以上穩定度、延遲、重連；4K60 本次未通過 |
| USB 雲台控制 | 本機列出 pan/tilt、zoom、roll；小幅 pan/tilt 寫入造成畫面位移，且能與影音並行 | 精確單位與回讀意義、定位容差、真正中途停止、重複性；zoom/roll 尚未動作測試 |
| Wi-Fi 影像與控制 | Kaze 有 Pocket 3 手機端作者實測與分級協議；Python 專案可供 macOS 對照 | 本機尚未測試；不同專案的軸向／旗標存在衝突，需固定版本驗證 |
| BLE 雲台控制 | 有配對、姿態與命令實作；官方 Osmo BLE 範例未列 Pocket 3 | 部分專案明說 BLE 單獨送命令會被忽略，不能當成功方案 |
| 收音 | 官方支持 Webcam 收音；本機取得 48 kHz、16-bit、雙聲道非零 PCM，與影像／控制並行 | 實體聲音來源、可懂語音、影音同步；沒有三路原始麥克風資料證據 |
| 全天候使用 | 官方允許外接供電，本次未做耐久 | 實際耗電、電量／溫度趨勢、主機休眠、斷流及恢復；2.5 W 配置值不是量測 |

不要預先承諾精確角度控制、所有模式共存、三路原始麥克風資料、聲源定位或 24/7 穩定。工具存在、命令成功送出、硬體完成動作，必須分開記錄。

**架構**

```text
MCP 客戶端                 最小 LLM 示範／未來 Yunmo
    │                              │
    │ MCP tools                    │ function calling／直接調用
    └──────────────┬───────────────┘
                   ▼
             共用相機控制核心
          能力查詢／取像／移動／停止
                   │
           實測支持的傳輸後端
          USB 優先，Wi-Fi 作備案
                   │
                   ▼
             DJI Osmo Pocket 3
```

核心與模型供應商解耦。MCP 只負責把相機能力暴露給客戶端；function calling 適配把模型工具請求映射到相同核心函式，避免重複實作控制邏輯。

已用 Swift AVFoundation 與編譯後的 uvc-util 做過短測。優先實作小型 USB 原生控制輔助程序，沿用共用核心管理影像、控制與錯誤；保留現有探測命令作驗證基線。初期不因跨平台願景提前搭建大型插件系統，也不為多家模型供應商各做一套示範。

**第一版接口草案**

這些是預定接口，須依實測修訂，不代表所有能力已通過驗收。

| 工具 | 用途 | 最低結果契約 |
|---|---|---|
| `camera_status` | 查詢連線及能力 | 連線狀態、裝置識別、後端、能力清單、最後錯誤；只回傳能取得的硬體資料 |
| `capture_frame` | 取得當下畫面 | 影像、frame_id、session_generation、尺寸、來源及時間戳來源；未知感測器時間時不冒稱曝光時間 |
| `move_gimbal` | 有界移動 | 僅接受能力清單允許的模式；回傳命令是否送出及動作是否有回饋確認 |
| `stop_gimbal` | 停止運動 | 優先於一般移動處理；若後端未驗證直接停止則標示不支持／未確認，不能把小幅定位結束當中途停止 |

若只能做速度與持續時間控制，就先提供相對移動；只有確認支持絕對角度及單位後，才增加絕對定位。本機已見 pan-tilt-abs 與視角變化，可優先研究絕對目標模式；回讀存在差值，需實測容差，不能直接承諾校準物理角度。角度、速度、持續時間與命令頻率皆須有界。斷線或控制租約逾時的停止行為需實測，不能只依賴模型記得呼叫 stop。

**里程碑 0：硬體可行性探測**

目標：確認「這台 Pocket 3 接這台 Mac，能否穩定取像、程式控制視角，且有可靠的停止／恢復行為」。

已完成：裝置與格式枚舉、1080p／4K 短時取像、雙聲道 PCM、小幅 pan／tilt 及恢復原設定、動作前後影格比對。環境為 macOS 27.0（26A5425a）、arm64、VID/PID 2ca3:0023。相機韌體仍未知，USB bcdDevice=0x0504 不當作韌體版號。

1. 記錄相機韌體、macOS 版本、主機架構、線材／連接方式及工具版本。
2. 擴展為 30 分鐘 USB 影音擷取；記錄影格間隔、主機負載、延遲和斷流。
3. 以已取得的 UVC 控制項與值域為起點，核對座標、鏡像、回讀語義與容差。
4. 每軸至少 20 次小幅往返；另做真正中途停止／取消測試，支持時才測回中。
5. 在影音與控制並行下測試錯誤、程序中止、斷線、休眠及重新連接；舊影格與舊動作失效。
6. USB 的停止／可靠性缺口無法解決時，再驗證 Wi-Fi；優先對照 Kaze 固定協議版本。USB＋正常 Mimo 控制不屬官方支持路徑，也不要假設所有無線模式都能共存。

交付：一份 `HARDWARE_VALIDATION.md`、可重現命令、短影片或動作前後影格，以及成功／失敗對照表。

通過條件：至少一種路徑能持續取像並完成可確認的有界移動與停止。若取像成功但轉頭不可用，就發布探測結果，將產品能力收斂為唯讀相機接入；不為證明原定構想而無限投入逆向工作。

**里程碑 1：最小可發布版本**

目標：控制核心＋四個工具＋一個示範，形成完整且獨立的成果。

示範任務：

> 「往左看一點，告訴我桌面上有什麼。」

示範必須真的取畫面、執行動作、取得更新後畫面並回答；硬體失敗時顯示失敗，不能生成假成功。若要展示「多了什麼」，必須先建立同一觀察區域的先前畫面，避免把視角改變當成物品新增。

交付內容：

- 可獨立調用的相機核心與簡單診斷 CLI。
- 使用同一核心的 MCP server。
- 一個直接模型 API 的 function calling 範例，與核心分開安裝／設定。
- 不接實機也能跑的接口測試；實機測試需明確啟用。
- README、安裝步驟、能力矩陣、限制、最小示範影片。
- 在選定授權後發布早期版本；公開前確認引用或移植程式的授權相容性。

驗收：乾淨檢出可按說明執行；沒有 Yunmo 也能使用；工具能區分不支持、斷線、逾時及成功；模型工具呼叫輪數、影格數與示範花費都有上限。

**里程碑 1.1：觀察任務強化**

由一個實際場景選擇功能，不要求全部同時完成：

- `look_at`：以新鮮 frame_id 和影像座標指定目標，小幅修正並取像確認。
- 區域命名與返回：先量測重複性；相機底座、變焦、鏡像或 session 改變時重新驗證。
- 同視角前後比較：先確認重疊和對齊，不把相機移動或曝光改變當物品變化。
- 4K 局部裁切：按需要提供細節圖，控制每個任務的影格和呼叫預算。

先作核心組合函式與示範，必要時才增加 MCP tools；選 10 個可人工核對的任務衡量成功率與成本。

**里程碑 2：聽覺接入**

第一版視覺與轉頭成立後才實作語音產品功能。硬體探索已提前證明 USB PCM 可取得；不重做已完成枚舉，繼續驗證聲音來源與同步。

- 確認音訊裝置枚舉與來源，不把 Mac 內建麥克風誤認為 Pocket 3。
- 驗證影音並行及時間戳；無線原始影像方案不能預設附帶音訊。
- 支持有長度上限的音訊片段擷取。
- 增加本地語音活動偵測，只有有語音時才觸發後續處理。
- 語音轉文字是獨立適配層，可替換供應商與本地實作。
- 不把聲源定位列入此里程碑，除非已驗證可取得足夠的音訊通道與校準資訊。

交付：有界的音訊擷取接口，以及「聽到要求 → 取像 → 回答」示範。

**里程碑 3：長時間感知與 Yunmo 接入**

先做耐久驗證，再考慮常駐自主感知：2 小時 → 8 小時 → 更長測試。以設備實際支持的低負載影音規格開始，記錄電量、發熱觀察、斷流、延遲與復原；不得宣稱目前已支持全天候。

本地感知程序負責便宜的事件偵測，模型只處理值得理解的影格／聲音片段。相機一直開著，不等於模型一直推論。保留全域呼叫預算、事件冷卻、去重與來源新鮮度。

雲台移動期間須標記相機自身運動，避免整幅畫面變化觸發新的觀察，再形成轉頭與分析的無限循環。模型只選擇觀察意圖，連續馬達控制交給本地程序。

Yunmo 透過公開接口取得感知事件或調用相機，不把驅動和連線邏輯搬入 `YunmoDaemon`。即使 Yunmo 暫停，本專案仍應能維護和發布。

正式常駐需要持續運作的主機。Mac 筆電可用於開發與驗證，不重新要求它承擔全天候模型推論。

**第一版明確不做**

- 人格、情緒、長期記憶、自發興趣和主動陪伴。
- 另一套完整聊天產品、帳號系統或大型 Web Dashboard。
- 多攝影機、多品牌及多作業系統的全面支持。
- 無限制的自主巡視、長時間錄影保存或持續雲端影音分析。
- 尚未實測的精確定位、聲源追蹤及自動追人。
- 自訓大型模型。依新的產品提案，本機模型推論已納入 App：Foundation Models／MLX VLM，以及 Core AI 感知評估。

預設不保存長時間原始影音；示範只保留必要、由使用者主動選擇保存的資料。先以本地 MCP 使用為範圍，不在初版暴露遠端無認證控制服務。

**避免再次被大專案拖住的規則**

1. 每個里程碑都能單獨展示，並有清楚的通過／停止條件。
2. 第一版完成一條觀察與動作流程就發布，不等待聽覺或全天候能力。
3. 新點子先記錄，只有直接妨礙本里程碑驗收的內容才進當前範圍。
4. 只宣稱已測的能力與硬體組合；MCP schema、SDK 方法或成功回傳碼不能代替實機證據。
5. Yunmo 的相容需求不能成為本專案發布門檻。

**參考專案與文件**

- [DJI：Osmo 系列 Webcam 說明](https://repair.dji.com/help/content?customId=01700006962&lang=en&paperDocType=ARTICLE&re=US&spaceId=17)：USB 取像與 macOS 支持的官方入口。
- [DJI Pocket 3 FAQ](https://www.dji.com/osmo-pocket-3/faq)：裝置、雲台與麥克風能力。
- [DJI Pocket 3 支持頁](https://www.dji.com/support/product/osmo-pocket-3)：供電與使用規格；不能延伸成全天候保證。
- [uvc-util](https://github.com/jtfrey/uvc-util)：macOS UVC 控制枚舉與設定；本輪已用於此台 Pocket 3 的讀寫短測，完整可靠性待驗收。
- [DJI-Wifi-Connect](https://github.com/sniffingpickles/DJI-Wifi-Connect)：Python／macOS 原型對照；部分命令未確認，停止函式與軸向定義需稽核。
- [Kaze for DJI](https://github.com/brianmerchant/Kaze-for-DJI)：Wi-Fi 協議主要參考，Pocket 3 手機端作者實測、命令證據分級及共享測試向量；仍為 alpha。
- [gaze](https://github.com/Obedience-Corp/gaze)：原生 UVC／MCP 相鄰專案；可學習架構，不代表本案支持機型。
- [DJI 韌體紀錄](https://dl.djicdn.com/downloads/DJI_Osmo_Pocket_3/RN/20250826/DJI_Osmo_Pocket_3_Release_Notes_en.pdf)：2024 年加入 4K UVC，優先於模糊的上市規格描述。
- [DJIRemote](https://github.com/TheRealShieri/DJIRemote)：Windows DirectShow 控制參考，不是 macOS 解決方案。
- [lib-osmo-ble](https://github.com/yigitkonur/lib-osmo-ble)：BLE 協議與姿態資料參考；作者明列 BLE 單獨控制馬達的限制。
- [dji-osmo-ble-protocol](https://github.com/coolboy/dji-osmo-ble-protocol)：BLE 配對、Wi-Fi 設定與 RTMP 研究；與前述 BLE 專案有 fork 關係，CLI／README 有不一致，不當作獨立成功證據。
- [FFmpeg AVFoundation](https://ffmpeg.org/ffmpeg-devices.html#avfoundation)：Mac 影音裝置列舉及擷取入口。

以上是調研入口，不是已選定依賴。採用時應固定版本並查核授權及現行相容性。

**下一個具體動作**

以現有 [硬體短測](HARDWARE_VALIDATION.md) 為基線，補上韌體紀錄、重複定位、中途停止及 30 分鐘影音測試。通過後實作 USB 控制核心＋四個工具＋一條可驗證的觀察任務，暫不擴大 Wi-Fi 逆向或 Yunmo 範圍。
